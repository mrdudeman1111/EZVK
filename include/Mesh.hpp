#pragma once

// #include <EkTypes.h>

// // glm::mat4 ConvertMat4(aiMatrix4x4 AssimpMatrix);

// struct Bone
// {
// public:
//     alignas(64) glm::mat4 RelativeTransform = glm::mat4(1.0f);
//     std::string Name = "";
//     Bone* Parent = nullptr;
//     glm::mat4 GetAbsoluteTransform();
//     glm::mat4 GetTransform() { if(this){return RelativeTransform;} else {throw std::runtime_error("Something tried to call GetTransform() on a bone that doesn't exist, check your callstack to see what called this");} }
// };

// class Mesh
// {
//     public:

// }

// class MeshLoader
// {
//     public:
// }
